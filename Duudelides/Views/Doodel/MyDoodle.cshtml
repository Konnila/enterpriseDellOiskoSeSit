@using System.Globalization
@model Duudelides.ViewModels.DoodleModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("MyDoodle", "Doodel", FormMethod.Post))
{
    var amount = 0;
    while (Model.Beginning > Model.Ending)
     {
         @Model.Ending
         @Html.RadioButton(Model.Beginning.ToString(CultureInfo.InvariantCulture),false)

         Model.Ending = Model.Ending.AddDays(1);
     }
    <ul>
        
    </ul>
    
}